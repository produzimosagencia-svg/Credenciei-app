{
  "name": "@credenciei/dominio",
  "version": "0.1.0",
  "description": "A regra de negócio do Credenciei — a mesma no servidor, na web e no celular.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "teste": "tsx --test src/*.teste.ts",
    "tipos": "tsc --noEmit"
  },
  "dependencies": {
    "@noble/curves": "^1.9.7",
    "@noble/hashes": "^1.8.0"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
