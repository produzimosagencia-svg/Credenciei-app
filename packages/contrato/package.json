{
  "name": "@credenciei/contrato",
  "version": "0.1.0",
  "description": "O que o aplicativo pode pedir ao servidor — e o que ele nunca pode.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "teste": "tsx --test src/*.teste.ts",
    "tipos": "tsc --noEmit"
  },
  "dependencies": {
    "@credenciei/dominio": "*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
